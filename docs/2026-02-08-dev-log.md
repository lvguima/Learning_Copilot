# Dev Log — 2026-02-08

## Summary

Today we completed the first post-v0.1 iteration kickoff and shipped high-value usability upgrades:

- Desktop app can auto-start backend sidecar.
- User can pick workspace folder from desktop dialog.
- Scan result supports file-level selection for context injection.
- Runtime model settings can be viewed/updated from UI.
- Config changes persist to `backend/config.yaml`.

## Implemented Changes

### 1) Desktop sidecar lifecycle (Tauri)

- Added folder picker command in Tauri.
- Added backend health check on startup (`127.0.0.1:8008`).
- Added backend auto-start logic with Python executable fallback:
  - `backend/.venv/Scripts/python.exe` first
  - fallback to `python` or `LRC_PYTHON_EXEC`
- Added graceful backend process cleanup on app exit.

Main file:
- `src-tauri/src/main.rs`

### 2) Backend runtime config API

- Added:
  - `GET /config`
  - `POST /config`
- Added config persistence helper.
- Added model compatibility helpers for pydantic v1/v2 style dump/validate.

Main files:
- `backend/local_review_copilot/server.py`
- `backend/local_review_copilot/config.py`
- `backend/local_review_copilot/models.py`

### 3) Frontend UX upgrade

- Added new app shell with:
  - directory picker button
  - scan file list + checkbox selection
  - selected file count display
  - model settings panel (provider/model/endpoint/timeout/context/API env key)
- Wired selected file list to `Chat/Review/Quiz` requests.

Main files:
- `frontend/src/AppV2.jsx`
- `frontend/src/main.jsx`
- `frontend/src/styles.css`

### 4) Docs and startup adjustments

- Updated backend startup references to `local_review_copilot.server:app`.
- Updated README with auto-sidecar instructions and config endpoints.
- Added iteration task document for next cycle.

Main files:
- `README.md`
- `backend/local_review_copilot/main.py`
- `backend/tests/test_api.py`

## Validation Notes

In this environment:

- Backend tests are blocked by local Python 3.8 + pydantic v1 mismatch (project target is Python 3.11+).
- Frontend `vite build` hit local sandbox process permission issue (`spawn EPERM`).

Functional runtime verification was done through local dev run (`cargo tauri dev`) and interactive checks.

## Known Risks / TODO

- Need full regression in your Python 3.11 virtual environment.
- Need final pass for text encoding consistency in Chinese UI labels and exported markdown.
- Need cleanup of temporary/legacy files and caches before packaging.

