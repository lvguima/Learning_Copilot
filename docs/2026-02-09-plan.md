# Plan — 2026-02-09

## Goal

Stabilize today’s new capabilities and move toward a daily-usable v0.1.1 build.

## Priority P0 (must finish)

1. **Environment-aligned validation**
   - Run backend tests in Python 3.11 venv.
   - Run desktop app end-to-end with sidecar auto-start.
   - Verify `GET/POST /config` behavior and persistence.

2. **Critical bug fixing**
   - Fix any startup failures in sidecar lifecycle.
   - Fix any folder picker integration issue.
   - Fix config save/load edge cases.

3. **Encoding and output consistency**
   - Normalize Chinese text display in frontend.
   - Normalize quiz/review markdown export text.

## Priority P1 (high value)

1. **Session Replay page (first usable version)**
   - Input trace_id
   - Fetch and display `/session/{trace_id}`
   - Basic filter for mode/time

2. **Export Center (first usable version)**
   - Show recent export files
   - Open local file path quickly

3. **Error UX unification**
   - Standardize API error parsing and presentation
   - Add user-facing actionable hints

## Priority P2 (if time allows)

- Add one-click “run review with selected files” shortcut from scan area.
- Add minimal loading skeletons for key cards.

## Acceptance Criteria (tomorrow close)

- Desktop app starts with no manual backend terminal.
- Scan → select files → Review/Chat/Quiz works in one flow.
- Config changes persist and apply to new requests.
- At least one backend test run passes in Python 3.11 env.
- No visible major Chinese text corruption in UI/export outputs.

