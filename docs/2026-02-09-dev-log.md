# Dev Log - 2026-02-09

## Scope

Today focused on P0 stabilization and validation:

- backend config persistence and safety hardening
- context parsing warning visibility
- frontend error readability and scan list usability
- backend test reliability in this environment

## Implemented

### 1) Backend config safety + path consistency

- `CONFIG_PATH` now resolves to `backend/config.yaml` from module path, not current working directory.
- `POST /config` now:
  - validates `workspace_root_dir` exists and is a directory
  - validates LLM payload before applying
  - rolls back in-memory config if save fails
  - returns absolute `saved_to` path

Files:
- `backend/local_review_copilot/server.py`

### 2) Context parsing warnings exposed to clients

- Added parse warning aggregation for selected files (`degraded`, `failed`, `image_only`).
- Added warning response field to:
  - `POST /chat/session`
  - `POST /review/run`
  - `POST /quiz/generate`
- Stored warning list in session record `errors` for replay/trace.

Files:
- `backend/local_review_copilot/server.py`

### 3) Export text consistency cleanup

- Removed duplicate quiz markdown assignment and standardized quiz export title/content in Chinese.
- Standardized review markdown headings in Chinese.

Files:
- `backend/local_review_copilot/server.py`

### 4) Backward compatibility cleanup for legacy app entry

- Simplified `local_review_copilot.app` to a compatibility wrapper that re-exports `server.app`.

Files:
- `backend/local_review_copilot/app.py`

### 5) Frontend UX improvements

- Unified request timeout and error normalization with actionable messages.
- Added warning panel rendering for backend parse warnings.
- Enhanced scan list item display with type + size.
- Updated selected-file count copy to clearer Chinese wording.

Files:
- `frontend/src/AppV2.jsx`
- `frontend/src/styles.css`

### 6) Test harness improvement

- Replaced `tmp_path` usage with repo-local fixture (`local_tmp_dir`) to avoid OS temp permission conflicts in this environment.
- Added test for invalid workspace rejection on `POST /config`.

Files:
- `backend/tests/conftest.py`
- `backend/tests/test_api.py`
- `backend/tests/test_scanner.py`

## Validation

- `python -m pytest backend/tests -q` -> **5 passed**
- API probe via `TestClient`:
  - invalid workspace update -> **400**
  - valid update `saved_to` -> `D:\pyproject\LearningCopilot\backend\config.yaml`
- `npm run build` (frontend) -> blocked by sandbox process permission (`spawn EPERM`)
- `cargo check` (tauri) -> blocked by file permission in `src-tauri/target` (`os error 5`)

## Remaining Risks

- Full desktop end-to-end smoke still needs to be run on your local desktop runtime.
- Frontend and tauri build verification are blocked in this sandbox; should be re-validated on your machine.
